# Assumptions

1. The codebase is heavily reliant on EventStore as the single source of truth.
2. The user has explicitly stated NO backwards compatibility is required, freeing us to make hard schema and API changes.
3. The codebase must align with `docs/EventBased_Concept.md`.
