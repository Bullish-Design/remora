# Project Rules

1. Keep this project self-contained under `.scratch/projects/treesitter-node-persistent-ids/` until explicitly integrated.
2. Do not auto-enable writeback in default CLI flows.
3. Prefer deterministic, testable pure functions for extraction and ID parsing.
4. Maintain language-specific queries in `template/queries/<language>/`.
