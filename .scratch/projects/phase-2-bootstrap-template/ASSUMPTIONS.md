# ASSUMPTIONS

## Phase
- This project belongs to Remora Phase 2 (bootstrap-first architecture).

## Architecture
- `bootstrap/` is the source of truth for bootstrap-native tools, agents, and templates.
- Remora is consumed as a Python library dependency.
- Existing `agents/*` bundle configs and Grail `.pym` scripts are out of scope by default.

## Delivery
- Work proceeds in small, testable increments.
- Standard project tracking docs are kept current for compaction-safe resume.
